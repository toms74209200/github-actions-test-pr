name: Check Pull Request

on:
  pull_request:
    branches:
      - master

permissions:
  contents: read
  pull-requests: write

jobs:
  test:
    uses: ./.github/workflows/test.yml
    with:
      enable_pr_comment: true
    permissions:
      contents: read
      pull-requests: write